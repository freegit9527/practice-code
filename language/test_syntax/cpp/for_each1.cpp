#include "algostuff.hpp"

// please write the page number from now...

using namespace std;

int main(void) {
  vector <int> coll;

  INSERT_ELEMENTS(coll, 1, 9);

  for_each (coll.cbegin(), coll.cend(),
            )
}
